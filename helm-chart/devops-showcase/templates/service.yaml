apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-showcase.fullname" . }}
  labels:
    {{- include "devops-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-showcase.selectorLabels" . | nindent 4 }}
