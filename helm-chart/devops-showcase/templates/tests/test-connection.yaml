apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-showcase.fullname" . }}-test-connection"
  labels:
    {{- include "devops-showcase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-showcase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
